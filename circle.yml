machine:
  environment:
    IMPORT_PATH: "github.com/zqzca/back"

dependencies:
  pre:
    - go get github.com/kardianos/govendor
    - go get github.com/go-playground/overalls
    - go get github.com/mattn/goveralls
    - go get bitbucket.org/liamstask/goose/cmd/goose

  override:
    - mkdir -p "$GOPATH/src/$IMPORT_PATH"
    - rsync -azC --delete ./ "$GOPATH/src/$IMPORT_PATH/"
    - cd "$GOPATH/src/$IMPORT_PATH" && govendor sync

test:
  pre:
    - go vet ./...
    - createdb zqz-test
    - goose -env test up
    - goose -env test status

  override:
    - go test ./... -race

  post:
    - overalls -project=github.com/zqzca/back -covermode=count
    - goveralls -coverprofile=overalls.coverprofile -repotoken ${COVERALLS_TOKEN}

